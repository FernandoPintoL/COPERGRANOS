/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Consultas;

/*
import java.io.File;
import java.io.IOException;
import org.jfree.chart.ChartFactory;
import org.jfree.chart.ChartUtils;
import org.jfree.chart.JFreeChart;
import org.jfree.data.general.DefaultPieDataset;
*/

/**
 *
 * @author fpl
 */
public class PieChartGenerator {

    /*public static File createPieChart() throws IOException {
        // Crear el dataset
        DefaultPieDataset dataset = new DefaultPieDataset();
        dataset.setValue("Categoría A", 40);
        dataset.setValue("Categoría B", 30);
        dataset.setValue("Categoría C", 20);
        dataset.setValue("Categoría D", 10);

        // Crear el gráfico
        JFreeChart pieChart = ChartFactory.createPieChart(
                "Distribución de Categorías",
                dataset,
                true, true, false);

        // Guardar el gráfico como imagen temporal
        File chartFile = File.createTempFile("pie_chart", ".png");
        ChartUtils.saveChartAsPNG(chartFile, pieChart, 500, 400);
        return chartFile;
    }*/

}
